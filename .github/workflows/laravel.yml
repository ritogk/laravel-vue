name: Laravel

on:
  push:
    branches:
      - main

jobs:
  laravel-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: compose-run
        working-directory: ./back
        run: |
          docker-compose up -d
      - name: laravel setup
        run: |
          sleep 0
          docker exec -it b_lvs_php sh ./setup.sh
